# Runtime + test dependencies for the CI "test" job.
# Compiled to test.txt with hashes via: make pip-compile
mcp>=1.28.1
pydantic>=2.9,<3
pytest>=9.0.3,<10.0.0
pytest-asyncio>=0.24,<2.0
pytest-cov>=6.0.0,<8.0.0
interrogate>=1.7
# HTTP transport + OAuth 2.1 resource-server auth, and (evidence-pack)
# Ed25519 signing -- runtime deps imported by the http subpackage /
# signing module and exercised by the tests.
pyjwt[crypto]>=2.10,<3
httpx>=0.27,<1
starlette>=1.3.1,<2
uvicorn>=0.30,<1
cryptography>=48.0.1,<49
# Optional cloud-signing extras (aws=boto3, vault=hvac) and their mock
# (moto) -- imported lazily by cloud.py and exercised by the mocked
# tests for sign_pack_aws_kms / sign_pack_vault / export_pack_to_s3.
boto3>=1.34,<2
hvac>=2.0,<3
moto[s3,kms]>=5.0,<6
